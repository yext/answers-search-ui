<div class="yxt-FilterBox-container">
  {{#if title}}
    <div class="yxt-FilterBox-titleContainer">
      <div
        data-component="IconComponent"
        data-opts='{ "iconName": "elements" }'>
      </div>
      <span class="yxt-FilterBox-title">{{title}}</span>
    </div>
  {{/if}}
  {{#each filterConfigs}}
    <div class="js-yext-filterbox-filter{{@index}} yxt-FilterBox-filter {{#if @first}}yxt-FilterBox-filter--first{{/if}}">
    </div>
  {{/each}}
  {{#if showApplyButton}}
    <div class="yxt-FilterBox-controls">
      <button type="button"
              class="js-yext-filterbox-apply yxt-FilterBox-apply">
        {{applyLabel}}
      </button>
      {{~#if showReset ~}}
        <button type="button" class="js-yxt-FilterBox-reset yxt-FilterBox-reset">
          {{resetLabel}}
        </button>
      {{/if}}
    </div>
  {{/if}}
</div>
