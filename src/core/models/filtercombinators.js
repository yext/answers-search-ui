/** @module FilterCombinators */

/**
 * FilterCombinators is an ENUM for valid combinators for {@link Filters}.
 */
const FilterCombinators = {
  AND: '$and',
  OR: '$or'
};

export default FilterCombinators;
